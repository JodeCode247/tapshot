{
    "version": 2,
    "builds": [
        {
            "src": "tapshot_back/api/webhook",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "tapshot_back/api/webhook"
        }
    ]
}